function getNumberType(number){
    if(number>0)
        {
    console.log(`number ${number} is a Positive no`);
    }
    else if (number<0)
        {
        console.log(`number ${number} is a Negative no`);
        }
        else
        {
        console.log(`number ${number} is Zero`);
        }
            
    }
    
    
    getNumberType(-30);
    